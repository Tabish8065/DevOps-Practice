package com.blogging.dto;

public record RegisterDto(
                        String name,
                        String username,
                        String email,
                        String password) {
    
}
